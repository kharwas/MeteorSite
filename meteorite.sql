drop table if exists meteorite;
create table meteorite(
  name text,
  id integer,
  nametype text,
  recclass text,
  mass int,
  fall text,
  year text,
  reclat text,
  reclong text,
  geolocation text
);
